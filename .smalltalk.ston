SmalltalkCISpec {
  #loading : [
    SCIMetacelloLoadSpec {
      #baseline : 'AgenticBrowser',
      #directory : 'src',
      #load : ['Tests', 'WebUI-Tests', 'Scripting-Tests'],
      #onConflict : #useLoaded,
      #useLatestMetacello : false,
      #onWarningLog : true,
      #platforms : [ #pharo ]
    }
  ]
}
